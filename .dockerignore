# Compiled Rust artifacts — large, rebuilt inside the container
target/

# Python reference implementation — not needed to build the Rust binary
reference/

# Tests — not shipped in the image
tests/

# CI, docs, scripts — not needed in the image
.github/
scripts/
*.md
CLAUDE.md

# Git metadata
.git/
.gitignore

# Editor / OS junk
.claude/
.vscode/
.idea/
*.pdb
